name: Branch

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

concurrency:
  # GitHub concurrency keeps at most one running and one pending run per group.
  # Use ref_name for PRs so new commits cancel older PR runs on the same PR,
  # but key pushes by SHA so postsubmit runs do not cancel each other.
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && format('pr-{0}', github.ref_name) || format('push-{0}', github.sha) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  CI: true
  RUST_VERSION: "1.97.0"
  CARGO_BUILD_WARNINGS: deny
  GO_VERSION: "1.26.6"

jobs:
  #### Proxy jobs ####
  proxy-test:
    env:
      # Test binaries do not need debug symbols in CI. In particular, generating
      # and linking MSVC PDBs accounts for a large part of the Windows build.
      CARGO_PROFILE_TEST_DEBUG: 0
      # This target-specific setting has no effect on the Linux and macOS matrix
      # entries. lld-link is bundled with LLVM on the Windows runner and is much
      # faster than the default MSVC linker for these large test executables.
      CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER: lld-link.exe
    strategy:
      matrix:
        include:
        - os: blacksmith-4vcpu-ubuntu-2404
          name: "Proxy Test"
        - os: windows-2025
          name: "Proxy Test (Windows)"
        - os: macos-15
          name: "Proxy Test (Mac)"
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
        # The Rust build cache restores source mtimes from each path's history.
        fetch-depth: 0
    - name: Set up Rust build cache
      uses: ./.github/actions/rust-build-cache
      with:
        rust-version: "${{ env.RUST_VERSION }}"
        cache-key: proxy-test
        # PRs restore the default branch cache; only postsubmit rolls it forward.
        save: ${{ github.ref == 'refs/heads/main' }}
    - name: Start validation dependencies
      if: ${{ startsWith(matrix.os, 'blacksmith-') }}
      run: tools/manage-validation-deps.sh start
    - name: Test
      run: make test
      env:
        KEYCLOAK_AVAILABLE: ${{ startsWith(matrix.os, 'blacksmith-') && '1' || '' }}
        # Generate Cargo's per-crate build report for the artifact below.
        TIMINGS: true
    - name: Upload cargo timings
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: cargo-timings-proxy-test-${{ runner.os }}
        path: target/cargo-timings/cargo-timing*.html
        retention-days: 2
    - name: Stop validation dependencies
      if: ${{ always() && startsWith(matrix.os, 'blacksmith-') }}
      run: tools/manage-validation-deps.sh stop

  proxy-lint:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
    - name: Install Rust
      uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
      with:
        toolchain: "${{ env.RUST_VERSION }}"
    - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
      with:
        # Github cache will restore from 'main' OR the current branch.
        # If we cache PR branches, we may evict the 'main' cache and end up with no cache hits at all.
        # Ideally, we would have main be 'high priority' and not be evicted but I don't think thats possible.
        save-if: ${{ github.ref == 'refs/heads/main' }}
    - name: Generation
      run: make generate-schema check-clean-repo check-default-members
    - name: Lint
      run: make lint

  #### UI jobs ####
  ui-lint:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: ui
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
    - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
      with:
        node-version-file: ui/.nvmrc
    - name: Lint
      run: |
        corepack enable
        pnpm install --frozen-lockfile
        pnpm lint
    - name: End-to-end tests
      run: pnpm test:e2e
    - name: Upload Playwright traces
      if: failure()
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: playwright-ui-traces
        path: ui/test-results
        retention-days: 2

  #### Controller jobs ####
  controller-test:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
    - name: Set up controller Go build cache
      uses: ./.github/actions/controller-build-cache
      with:
        go-version: "${{ env.GO_VERSION }}"
        go-cache-name: controller-test
        # PRs can restore the default branch caches, but only postsubmit rolls them forward.
        save: ${{ github.ref == 'refs/heads/main' }}
    - name: Test
      run: go test -race ./...

  controller-lint:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
    - name: Set up controller Go build cache
      uses: ./.github/actions/controller-build-cache
      with:
        go-version: "${{ env.GO_VERSION }}"
        go-cache-name: controller-lint
        # PRs can restore the default branch caches, but only postsubmit rolls them forward.
        save: ${{ github.ref == 'refs/heads/main' }}
    - name: Lint
      run: make -C controller analyze
    - name: go mod tidy
      run: |
        go mod tidy
        git diff --exit-code go.mod go.sum
    - name: Gen
      run: |
        make generate-apis check-clean-repo
        make -C controller verify

  controller-e2e:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
        # git-restore-mtime needs full history to find each file's latest change.
        fetch-depth: 0
    - name: Set up controller build caches
      uses: ./.github/actions/controller-build-cache
      with:
        rust-version: "${{ env.RUST_VERSION }}"
        go-version: "${{ env.GO_VERSION }}"
        # PRs can restore the default branch caches, but only postsubmit rolls them forward.
        save: ${{ github.ref == 'refs/heads/main' }}
    - name: Setup
      run: TEST_MODE=e2e ./controller/test/setup/setup-kind-ci.sh
    - name: E2E
      run: |
        CGO_ENABLED=0 PERSIST_INSTALL=true go test -tags=e2e -v ./controller/test/e2e
    - name: Archive bug report directory on failure
      if: ${{ failure() }}
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: bug-report
        path: ./controller/_test/bug_report/kind
        retention-days: 2
    - name: Upload cargo timings
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: cargo-timings-e2e
        path: target/cargo-timings/cargo-timing*.html
        retention-days: 2
    - name: Summary
      run: |
        echo '# E2E Timings\n'
        cat controller/_test/ci-step-timings.log >> $GITHUB_STEP_SUMMARY

  controller-conformance:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
        # git-restore-mtime needs full history to find each file's latest change.
        fetch-depth: 0
    - name: Set up controller build caches
      uses: ./.github/actions/controller-build-cache
      with:
        rust-version: "${{ env.RUST_VERSION }}"
        go-version: "${{ env.GO_VERSION }}"
    - name: Setup
      run: TEST_MODE=conformance ./controller/test/setup/setup-kind-ci.sh
    - name: Conformance
      run: |
        make -C controller agw-conformance
    - name: Debug Info
      if: ${{ failure() }}
      run: |
        kubectl describe deployment -A
        kubectl get pods -A
        kubectl logs -n agentgateway-system -l app.kubernetes.io/name=agentgateway
    - name: Upload cargo timings
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: cargo-timings-conformance
        path: target/cargo-timings/cargo-timing*.html
        retention-days: 2
    - name: Summary
      run: |
        cat-yamls () {
          local files=("$@")
          local count=$#

          for ((i=0; i<count-1; i++)); do
          cat "${files[i]}"
          echo -e "\n---"
          done

          if [ $count -gt 0 ]; then
          cat "${files[count-1]}"
          fi
        }

        echo -e '\n# Conformance Reports\n\n```yaml' >> $GITHUB_STEP_SUMMARY
        cat-yamls ./controller/_test/conformance/*.yaml >> $GITHUB_STEP_SUMMARY
        echo -e '```\n' >> $GITHUB_STEP_SUMMARY
        echo -e '# Conformance Timings\n' >> $GITHUB_STEP_SUMMARY
        cat controller/_test/ci-step-timings.log >> $GITHUB_STEP_SUMMARY

  controller-gie-conformance:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
        # git-restore-mtime needs full history to find each file's latest change.
        fetch-depth: 0
    - name: Set up controller build caches
      uses: ./.github/actions/controller-build-cache
      with:
        rust-version: "${{ env.RUST_VERSION }}"
        go-version: "${{ env.GO_VERSION }}"
    - name: Setup
      run: TEST_MODE=conformance ./controller/test/setup/setup-kind-ci.sh
    - name: GIE Conformance
      run: |
        make -C controller gie-conformance
    - name: Debug Info
      if: ${{ failure() }}
      run: |
        kubectl describe deployment -A
        kubectl get pods -A
        kubectl logs -n agentgateway-system -l app.kubernetes.io/name=agentgateway
    - name: Upload cargo timings
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
      with:
        name: cargo-timings-gie-conformance
        path: target/cargo-timings/cargo-timing*.html
        retention-days: 2
    - name: Summary
      run: |
        cat-yamls () {
          local files=("$@")
          local count=$#

          for ((i=0; i<count-1; i++)); do
          cat "${files[i]}"
          echo -e "\n---"
          done

          if [ $count -gt 0 ]; then
          cat "${files[count-1]}"
          fi
        }

        echo -e '\n# GIE Conformance Reports\n\n```yaml' >> $GITHUB_STEP_SUMMARY
        cat-yamls ./controller/_test/conformance/*.yaml >> $GITHUB_STEP_SUMMARY
        echo -e '```\n' >> $GITHUB_STEP_SUMMARY
        echo -e '# GIE Conformance Timings\n' >> $GITHUB_STEP_SUMMARY
        cat controller/_test/ci-step-timings.log >> $GITHUB_STEP_SUMMARY
